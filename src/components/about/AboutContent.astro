---
import { marked } from "marked";
import { activeAboutItem } from "src/data/store.ts";

const { data, active, idx } = Astro.props;
// type data = {
//   id: "Title.mdx";
//   slug: "title";
//   body: "\nBody\n";
//   collection: "about";
//   data: { title: "Title"; snippet: "Snippet"; menu_label: "Menu Label" };
//   render: any; //[AsyncFunction: render]
// };
marked.setOptions({
  pedantic: false,
  // smartLists: true,
  // smartypants: true,
});
const body = marked.parse(data.body);
---

<div
  id={`post-${data.slug}`}
  data-idx={idx}
  class=`post md ${!active && "hidden"}`
  set:html={body}
/>
